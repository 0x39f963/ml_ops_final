Generated at: 2026-06-01 17:37:04 MSK

# Sanitize report

## Scope

- scanned public `ml005/` tree
- excluded runtime / ignored dirs from review: `.git`, `.venv`, `mlruns`, `models`, `data`
- binary outputs excluded from text grep: images / parquet / csv / html / pyc
- internal real names are not copied into this report

## Checks

| category | command pattern | result |
|---|---|---|
| company-name pattern | `rg -n -i <company-name-pattern> ml005 ...` | empty |
| real raw-product marker | `rg -n -i <raw-product-marker> ml005 ...` | empty |
| standalone 10/12 digit id pattern | `rg -n '(^|[^A-Za-z0-9_.])[0-9]{10}...` over md/py/ipynb | empty |
| sensitive-value words | `rg -n -i <credential-like-pattern> ml005 ...` | reviewed, no real credential |
| masking lint | `grep -rnP '[\\x{0451}\\x{0401}\\x{2014}]' ml005 --include='*.md' --include='*.ipynb' ...` | empty |
| git anti-leak status | `git -C ml005 status --porcelain | grep -E '\\.pkl$|\\.joblib$|\\.parquet$|mlruns|\\.env$'` | empty |
| gitignore gate | `git -C ml005 check-ignore -v data/real.parquet ... .env` | matched all required local-only paths |

## Reviewed non-sensitive matches

- compose / Airflow demo credentials: `admin/admin`, `minioadmin/minioadmin` -> [assumption] allowed for local demo stack
- Grafana / MinIO placeholder env keys -> demo defaults only, real values stay in `.env`
- hashing code uses a local seed string -> deterministic hash input, not a credential
- CI docs mention credential policy only, not values

## Gitignore evidence

```text
.gitignore:7:data/**/*.parquet data/real.parquet
.gitignore:8:data/feature_store/ data/feature_store/q=2022Q1/part.parquet
.gitignore:17:*.pkl models/model.pkl
.gitignore:18:*.joblib models/model.joblib
.gitignore:19:mlruns/ mlruns/0/meta.yaml
.gitignore:25:.env .env
.gitignore:29:*/__pycache__/ src/__pycache__/x.pyc
```

## Notes

- `data/sample_synth.parquet` is synthetic and allowed by `.gitignore`.
- MDD latency CSV files are synthetic but ignored because they are large and regenerated by notebook.
- browser screenshots pending are listed in `screenshots/INDEX.md`; no missing screenshot was fabricated.

**Вывод:** clean. No company-name pattern, no standalone id pattern, no real raw-product marker, no real credential, masking lint empty, and local data/model/env/mlruns paths are gitignored.
